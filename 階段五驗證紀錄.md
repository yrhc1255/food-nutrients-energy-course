# 階段五驗證紀錄

日期：2026 年 9 月 20 日

## 發布架構

- GitHub：`https://github.com/yrhc1255/food-nutrients-energy-course`
- GitHub Pages：`https://yrhc1255.github.io/food-nutrients-energy-course/`
- GAS：`https://script.google.com/macros/s/AKfycbypwIxx63lAS_lu1t44-t-33Anv6sWFOu3BzUZZw0l4fD677XZ2Uil9iFOyyghFyBziig/exec`
- Google 試算表：儲存在 `yrhclee@gmail.com`，名稱為「食物中的養分與能量＿學習成果」；私人網址不寫入公開儲存庫。
- Firestore：前端未連接，成績不傳送至 Firestore。

## GAS 實測

以「測試班／05／階段五測試」與 `testMarker=true` 測試：

1. 首次寫入成功，回傳 `duplicate=false`。
2. 相同 `eventId` 再送一次，回傳 `duplicate=true`。
3. 以新 `eventId` 更新同一學生，總表仍只有一列。
4. 最新形成性評量由 88 更新為 95；較低的遊戲一 60 未覆蓋原最高分 73；較低的極限挑戰 900 未覆蓋原最高分 1200。
5. 執行 `cleanupTestData` 後重新讀取，測試列不存在，只保留標題列。

## 自動檢查

`npm test`：12 項通過，0 項失敗。涵蓋 15 頁結構、10 個學習頁各 5 種題型、計分、選項洗牌、身分切換、作答狀態與成果送出條件。

## 正式網站檢查

- GitHub Pages 建置狀態：`built`，版本 `64524db`。
- 正式首頁與 `sync-config.js` 均回傳 HTTP 200，正式頁已載入 GAS 網址。
- 390 × 844、768 × 1024、1440 × 900 三種視窗皆顯示 15 頁導覽與主要內容，沒有水平溢出。
- 正式頁瀏覽器主控台：0 個警告、0 個錯誤。
