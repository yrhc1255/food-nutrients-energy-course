# 階段三驗證紀錄

日期：2026 年 9 月 3 日。範圍為本機示範的首頁、導讀與碘液代表學習頁。

## 實際瀏覽器操作

| 情境 | 結果 |
| --- | --- |
| 首頁開始學習 | 班級、座號、姓名填妥後進入導讀；座號 05 正規化為 5 |
| 導讀尚未完成 | 下一頁停用；從進度列點碘液時顯示解鎖條件，未跳過導讀 |
| 導讀答錯 | 兩題作答後顯示訂正說明，不扣分，仍可解鎖代表頁 |
| 六養分探索 | 點水的功能卡，內容更新為不供能但仍必要的功能說明 |
| 預測與操作 | 預測未完成時不能滴加；先選錯水的預測，滴加後如實呈黃褐色，並提示預測差異 |
| 已知樣本比較 | 水呈黃褐色、澱粉液呈藍黑色，圖像、替代文字與觀察文字一致 |
| 觀察紀錄 | 兩個顏色均記錄後才可核對，正確紀錄有說明 |
| 未知樣本再應用 | A 與水比較；選「只有澱粉」出現推論範圍提醒，可再改選合理結論 |
| 五種題型 | 逐一操作單選、點選配對、步驟排序、是非、三部分證據拼圖；未完成不能檢查 |
| 同次作答穩定 | 選取單選時選項次序不變；重新載入後觀察到不同次序 |
| 錯誤後重新載入 | 未完成答案與回饋清空，仍顯示第 2 次作答，未重置得分資格 |
| 得分 | 一次完整測試依序得到 2、3、3、1、3 分，顯示 12／15，五題完成後才可查看成果 |
| 完成紀錄重載 | 前三題顯示保留 2、3、3 分；答案預設收合，不再給分 |
| 教師模式 | 錯誤密碼提示；55688 可進入，未做導讀仍能看已製作代表頁 |
| 教師隔離 | 教師作答得到 3 分；退出後原學生紀錄仍為 12／15、100% |
| 換人 | 只更改姓名，導讀恢復未完成、下一頁停用，不沿用前一人的紀錄 |
| 圖片放大 | 導讀餐圖可開啟、完整顯示並關閉；圖片無重要內容裁切 |
| 課程地圖 | 列出完整 19 頁；尚未製作項目停用，沒有連到不存在的頁面 |

## 排版與修正

- 已檢視電腦 1440 px、平板 768 px、手機 390 px 配置。手機實際操作配對、排序與證據拼圖，並檢視成果。
- 瀏覽器內容區寬度會扣除捲軸；手機 390 px 時讀得 clientWidth／scrollWidth 均為 375 px，平板 768 px 時均為 753 px。另檢查導讀在 320 px 時兩者均為 305 px。
- 修正手機浮動分數遮到下一題的問題，改為每題回饋區下方靠右顯示本次總分，且隨後續作答同步更新。
- 圖片完整顯示，正文在窄螢幕改為單欄，題型操作不依賴拖曳或滑鼠懸停。
- 最後讀取瀏覽器錯誤紀錄為空。

## 自動檢查

`node --check app.js` 通過；`node --test tests/state.test.mjs` 共 5 組測試通過。

測試涵蓋：刷新後保留嘗試、重送不重複加分、第三次起得 1 分、五題完成判定、任一身分欄位改變、正規化、完整作答條件、內容 ID 判分、排序、洗牌不修改原陣列及無效紀錄復原。

## 邊界

- 這些是桌面瀏覽器調整尺寸及操作的結果，尚未由實體手機或平板觸控驗證。
- 本機靜態示範沒有遠端帳號、伺服器判分、正式答案保護、跨裝置續學或正式成績回傳。正式資料層須待階段五授權。
- 本次沒有建置工具；直接提供靜態檔案與模組，因此不以不存在的「正式建置通過」作驗收聲明。
- 測試使用虛構姓名，最後切換為未完成的「範例同學」。沒有真實學生資料或任何雲端測試殘留。
